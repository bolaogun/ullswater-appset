apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ullswater-senzing-stack.fullname" . }}-test-connection"
  labels:
    {{- include "ullswater-senzing-stack.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ullswater-senzing-stack.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
